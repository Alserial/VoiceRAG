# ============================================
# VoiceRAG 环境变量配置示例
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 填入实际配置值
# ============================================

# ============================================
# Azure Communication Services (ACS) 配置
# ============================================
# 用于接收和处理电话来电（测试 ACS 功能必需）
ACS_CONNECTION_STRING=endpoint=https://xxx.communication.azure.com/;accesskey=xxx
ACS_CALLBACK_URL=https://your-ngrok-url.com/api/acs/calls/events
ACS_PHONE_NUMBER=+1234567890


# 启用后仅允许带 ?session=... 的 /realtime 连接（推荐 ACS 压测/生产时开启，避免前端并发占用 realtime 配额）
ACS_ONLY_MODE=false
# 注意：欢迎语音使用 ACS Call Automation 内置的 TTS 功能
# 不需要额外配置，代码中已设置默认欢迎文本
# 如需修改欢迎文本，编辑 test_acs_server.py 中的 play_welcome_message 函数

# ============================================
# Azure OpenAI 配置（语音交互功能）
# ============================================
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_REALTIME_DEPLOYMENT=gpt-4o-realtime
AZURE_OPENAI_REALTIME_VOICE_CHOICE=alloy
AZURE_OPENAI_EXTRACTION_DEPLOYMENT=gpt-4o-mini

# ============================================
# Azure AI Search 配置（RAG 搜索功能）
# ============================================
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_API_KEY=your-search-api-key
AZURE_SEARCH_INDEX=your-index-name
AZURE_SEARCH_SEMANTIC_CONFIGURATION=your-semantic-config
AZURE_SEARCH_IDENTIFIER_FIELD=chunk_id
AZURE_SEARCH_CONTENT_FIELD=chunk
AZURE_SEARCH_EMBEDDING_FIELD=text_vector
AZURE_SEARCH_TITLE_FIELD=title
AZURE_SEARCH_USE_VECTOR_QUERY=true

# ============================================
# Azure Storage 配置（文档存储）
# ============================================
AZURE_STORAGE_ENDPOINT=https://yourstorage.blob.core.windows.net
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=xxx;AccountKey=xxx;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER=your-container-name

# ============================================
# Salesforce 配置（报价功能 - 可选）
# ============================================
SALESFORCE_INSTANCE_URL=https://your-instance.salesforce.com
SALESFORCE_USERNAME=your-username@example.com
SALESFORCE_PASSWORD=your-password
SALESFORCE_SECURITY_TOKEN=your-security-token
SALESFORCE_CONSUMER_KEY=your-consumer-key
SALESFORCE_CONSUMER_SECRET=your-consumer-secret
SALESFORCE_DEFAULT_PRICEBOOK_ID=01sXXXXXXXXXXXXXXX
SALESFORCE_CREATE_OPPORTUNITY=false

# ============================================
# Email 配置（发送邮件 - 可选）
# ============================================
EMAIL_SERVICE=smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true
EMAIL_FROM=your-email@gmail.com
EMAIL_FROM_NAME=VoiceRAG System
EMAIL_SUBJECT_PREFIX=

# ============================================
# Teams Calling 配置（可选）
# ============================================
TEAMS_TENANT_ID=your-tenant-id
TEAMS_CLIENT_ID=your-client-id
TEAMS_CLIENT_SECRET=your-client-secret
TEAMS_BOT_APP_ID=your-bot-app-id
TEAMS_BOT_DISPLAY_NAME=VoiceRAG Bot
TEAMS_CALLBACK_URL=https://your-app.com/api/teams/callbacks

# ============================================
# Azure 通用配置
# ============================================
AZURE_TENANT_ID=your-tenant-id

# ============================================
# 应用配置
# ============================================
RUNNING_IN_PRODUCTION=false
HOST=0.0.0.0
PORT=8765

